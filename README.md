# Hello React

A basic React app with a centered hello message and a button that shows a `ready` alert.

## Run locally

```sh
npm install
npm run dev
```

Open the local URL printed by Vite.

## Production build

```sh
npm run build
npm run preview
```
